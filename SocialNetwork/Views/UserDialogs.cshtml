@model IEnumerable<SocialNetwork.ViewModels.DialogsListItemViewModel>

@{
    ViewData["Title"] = "UserDialogs";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>UserDialogs</h2>

<p>
    <a asp-controller="UserDialogs" asp-action="Create">Create New Message</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DialogID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DialogName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastMessageText)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DialogID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DialogName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastMessageText)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
